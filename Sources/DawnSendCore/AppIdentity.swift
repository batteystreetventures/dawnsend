/// Stable product identity used by the app, packaging, and tests.
public enum AppIdentity: Sendable {
    public static let displayName = "DawnSend"
    public static let bundleIdentifier = "app.dawnsend"
    public static let tagline = "Keep the Mac awake. Hit Send at the time you choose."
    public static let minimumSystemVersion = "13.0"
    public static let version = "0.1.0"
}
